name: step_progress
description: A Flutter widget for displaying step-by-step progress.
version: 0.0.2
homepage: https://github.com/TalebRafiepour/step-progress
issue_tracker: https://github.com/TalebRafiepour/step-progress/issues

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
