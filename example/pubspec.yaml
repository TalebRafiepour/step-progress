name: example
description: A demo project for showcasing the capabilities of the StepProgress package.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  step_progress:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true